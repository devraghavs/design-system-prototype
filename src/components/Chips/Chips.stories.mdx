import { Meta, Story } from '@storybook/addon-docs';
import Chips from './Chips';

<Meta title='XT-UI-Kit-Components/Chip' component={Chips} />

export const Template = (args) => <Chips {...args} />;

<Story
  name='Chip'
  args={{
    chipVariant: 'success',
    label: 'Label',
    disabled: false,
    isSelected: false,
    isRemoveable: true,
    dataTestId: '',
  }}
>
  {Template.bind({})}
</Story>
